{
  "name": "picture-map",
  "private": true,
  "main": "main.js",
  "type": "module",
  "version": "1.0.0",
  "license": "ISC",
  "scripts": {
    "start": "nodemon --exec electron .",
    "build": "electron-builder",
    "build:auto": "npm run build:backend && npm run build:frontend && electron-builder && node clean-node-modules.js",
    "build:backend": "cd picMap_backend && npm run build",
    "build:frontend": "cd picMap_fontend && npm run build",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd picMap_backend && npm start",
    "dev:frontend": "cd picMap_fontend && npm run dev",
    "install:all": "npm install && cd picMap_backend && npm install && cd ../picMap_fontend && npm install"
  },
  "build": {
    "asar": false,
    "appId": "picture-map",
    "productName": "picture-map",
    "files": [
      "main.js",
      "preload.js",
      "picMap_backend/dist/**",
      "picMap_backend/node_modules/**",
      "picMap_backend/package.json",
      "picMap_fontend/dist/**",
      "package.json"
    ],
    "electronLanguages": [
      "zh-CN",
      "en-US"
    ],
    "nsis": {
      "oneClick": false,
      "allowElevation": true,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "picture-map"
    },
    "directories": {
      "buildResources": "build"
    },
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ]
    }
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "electron": "^36.4.0",
    "electron-builder": "^26.0.12",
    "nodemon": "^3.1.10"
  }
}